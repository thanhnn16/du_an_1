<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.DuAn1" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/quicksand</item>
    </style>

    <style name="Theme.DuAn1" parent="Base.Theme.DuAn1" />

    <style name="Theme.DuAn1.CustomSplashScreen" parent="Theme.SplashScreen">
        <item name="statusBarBackground">@color/transparent</item>
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.DuAn1</item>
        <item name="windowSplashScreenAnimationDuration">3</item>
    </style>

</resources>